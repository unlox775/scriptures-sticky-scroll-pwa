This spec marks a pivot point. The interface-refinements work (Prompts 1–8) was not making sufficient progress. From here forward, iterate in this spec.

=== Prompt 1 (URL/state, layout, reader bubble, scroller, debug mode) ===

URL/state persistence: Add hash-based routing (#/w/:workId, #/b/:workId/:bookId, #/r/:workId/:bookId/:chapter/:verse) so refresh and hot-reload preserve the current view. Use localStorage as fallback for PWA when URL might reset.
Layout: Add padding so content isn't smashed against the top bar on each view.
Reader: Remove the bordered/rounded "bubble" around the reader content—make it full-bleed, no clipping at bottom.
Scroller: Fix jumpToLocation so opening 2 Ne 4 actually shows chapter 4 (scroller positioning bug).
Auto-scroll bar: Ensure it only shows in reader when auto-scroll is active.
Developer mode: Easter egg—tap top-left 10×10px zone on home screen to enable. Bug icon at bottom-right opens a drawer with Storage (localStorage explorer) and Logs. Comprehensive logger for anchor changes, scroll velocity, auto-follow, etc. Copy button exports logs as JSON to textarea (auto-select for paste). Session-based logs with 7-day purge.

=== Prompt 2 (critical paths, AI-human visibility docs, refactors) ===

Okie dokie. I would like you to examine this app. I'm going to describe it basically in generic terms to see if overall the values I'm trying to communicate are things that... you can apply to any application. So I'm literally thinking of two different applications in mind here. And you don't know which one it is. So theoretically, this is all stuff that applies to this application. In general, The natural thing that happens when you talk to an AI agent or even just an actual web developer and tell them what things you want to make. We'll start with the simplest framework as possible. In this case, revealing that this is a progressive web app that is loaded on mobile phones. Generally, iOS is the common thing, but progressive web app loads on phones. and this hypothetical app, um, is working. It's doing the things that it's supposed to do. There are things we want to keep developing on it and changing on it. But in general, the agent in the past, or agents in the past that have developed on this, haven't radically changed or improved the structure at all. um in some cases there's a more of a back-end set of modules that uh i still encourage the idea of having a set of things to keep track of um you know important objects uh things that can be abstracted away from the user interface is what I call back-end stuff. And I prefer to have those as modules defined and broken out into separate groups. This app may or may not have that broken out, but I want you to consider that part of it. The idea is if they are... Things being handled, data being processed, there almost always is some... There's state itself, there's logging. These are generic parts of any application. And so essentially you need to have a discipline. on what things you're breaking out on the back end. I personally think that when you come up with one or more of these, there's generally two types. environmental harnesses you know one module it's a back-end thing but lots of things use it and there might be two of these ones like you know one for database stuff one for logging or something But there are things that might get tapped by lots of other things. But then there's modules that just own a specific thing in a microservice world on a cloud. These would be separate microservices. And each one of these modules has its own interface. And nothing should violate these contracts as the interfaces to these modules. So the first is to figure out the overall task we're trying to do here. I think it's probably good to have a lot of the stuff here represent based off of design pattern, based off of the... primary user critical path the user experience on what we expect people to do most of the time so when a user comes to the application based off of the stated goals of the application what is the most common thing if they do this what is the most complete path that touches as deep into the application as is possible for the sake of making a single critical path, that if that one path works, this app essentially is justified in whatever its purpose is. That should be really well defined. What the experience looks like, how it should behave, the values of why it should behave the way it does. Ultimately there should be a some master integration test or something that is trying to follow that to make sure that that flow works. No matter what we do, that flow works. Now this critical path has front end user interface when you're running it, as well as the pieces you're interacting with are back end pieces. So the back end pieces and the front end pieces. Theoretically, by looking at the critical path, you should be able to essentially talk about the different parts of the system. For back-end pieces, it's a two-phase thing where, you know, I can say on step two of the critical path, you might be first phase as you're loading this user interface. That's going to refer to a front end. In this case, both of these progressive web apps are react based frontends in the progressive web app so the components are ultimately the frontend user interface pieces here so the first phase is what page you know or what component is being viewed potentially what sub components are in there as well involved in this, you know, step or phase, what's on the page, what the user interacts with. Only talking about major components here, you know, I don't want to go down to the fine grain of like buttons and forms and inputs and stuff like that, but just, you know, what are the master components that are part of this critical path? And then the backend phase is that those components probably have a one or more of these backend modules that they're going to connect to. And that phase of the front end connecting to the backend should always be using this API contract that the backend module has. So, the front end, each one of the steps in the critical path will probably have a front end aspect. Sometimes there'll be no back end involved other than, you know, the base things of logging and those ones that are... environmental things like database access, persistence of session or data storage, or just general logging, debugging tools. They probably shouldn't be discussed as part of the critical flow. They're just kind of the plumbing that exists on the back end. But all... other modules, that should be the critical path. And I literally want the critical path to be walked through like a story. You know, something about... you know, the homepage, it loads this experience for the user. You know, it's a landing page that might list something or something. And then there's a critical path might be to create a new something and that would be the front end flow and page for creating something and you could say it would you know need that component to load so that it can show the input for the input of whatever the thing is or whatever experience it is to create one of these things the app is a critical object in the front end and then just to say when it's done it will store the object with backhand piece. And this is literally like a story you could walk somebody through in... I'm thinking in the critical path, simplified view should just be a bulleted list. I'm thinking like a bulleted list, rich bulleted list item is a, you know, paragraph, paragraph, you know, 20 to 50 words, but they should start with a bolded section, which is the essence of what that step is, you know. Step one is something like interact to view the main loading page. Second one is to create or whatever the thing is. So there's a little bolded section at the beginning of each one of these things here. But that series of just should be a numbered list that can walk through the whole critical path. And reading that critical path should be a real quick, simple read. It should be talking about components. front-end components. It should be talking about back-end things they interact with other than the environmental ones. And it should walk through all the different pieces there. That's the first thing. Be able to create a comprehensive core critical path statement. all the way through. We should be able to basically name almost all the main objects that are involved in a critical path. And most of the backend objects are... They are probably going to be mentioned during a critical path. It's possible some of them only come into secondary and tertiary paths. So I think critical path and then at least one other path. There's lots of different things that are usable, totally valid cases. Maybe you have an app that isn't really super simple. There's lots of... different types of things you can be doing, either different phases of user usage. Maybe over the user's life cycle of it, there's an initial critical path to get them started. And then there's later on, maintenance or updates or follow-ups or reviews or something. And those would all be secondary, tertiary, quaternary. critical paths. So first, create all those critical paths and basically describing them all. This is part of the domain-driven design aspect of creating the ubiquitous language. By going through and defining this critical path, using these simple names here, we should have ubiquitous language for front-end and back-end components. So that's the first initial part I want you to go through and create on both of these applications is what is this? For right now, read through might be on there, but it may not be done with this discipline in place. See if you can follow this idea and go in and create this critical path, secondary path, tertiary, quaternary, rarely. But at least a secondary, you know. And the whole theme of these paths are, this is the one thing. If this thing works here, that adds most of the value that we want to give to the user. Secondary paths, like, yes, by adding the secondary path, we're still adding a great amount of value to do this second path. But these are full experiences that a user can experience. See, go through all the way from start to whatever the end page or view or interface is. Okay, that's phase one. Let's get all the critical paths defined, captured, the components. So critical path plus the definitions of what the different components are. So critical, secondary, tertiary. That probably should be in one standard document, the main paths. Then I think... in that same document, the second half after you go through those walkthroughs, should be some brief... walkthrough of what those other components are. And, you know, so if there's a back end, there's three different back end pieces. You should have three different things at the bottom to talk about these are the back end things. Once again, back end things are things that can kind of exist in an abstract sense. outside of the user interface. You know, if this is a database object or probably more collection of them, I don't like a one-to-one having a backend module that is one-to-one with like a table. Usually it's a critical business need or category of things that you can have a handful of and or API contract methods that ultimately help define what that backend is. thing is, is that they're kind of like subdomains, but they're back-end abstract things that are, you know, if you take this outside of the context of it being a progressive web app, or having user interface screens, or whatever, what are those back-end things? So, So define all those. All that stuff should be top half of it, critical path, secondary, tertiary paths if necessary. And then the bottom half of that document is a brief description of each one of them. If it is a back-end thing, maybe even if it's for a front-end thing, if there's main options, just a real kickstarter as to what the main API contract things are, but not exhaustive. What are the main aspects of that so someone can understand at a glance? What are the different, you know, if it's sub-UI components, I could list those. If there's model objects that are in there, I should list those. but the actual different AI methods for the main contract of those modules should be called out there. All right, so that's the general idea behind all of those. That gives the general thing for any application, whether it's built with AI or not. That's great. Second half. This is specifically a guideline I want to follow these enforcements, follow these rules, really good separation on any application that gets built. Second half, there should be a separate markdown file document. And this should be about AI to human beings. Visibility layer or something like that. Essentially, when a human asks someone to build an application, They nearly always just talk about the end result, the screens they want to have, and they don't know enough or usually aren't focused on what is essentially the debugging harnesses. This includes logging of different things, but overall is more of an abstract topic. This involves what I'm terming a Tor. Say that we envision all the different flows in here. There's back-end API methods. There's essentially things that happen through the course of the application. It could be a front-end thing, like a component on the front-end visual thing that it queries some backend thing. And they have the process of asking the backend to get this thing. It might not be a simple query lookup here. Or maybe it is just a query lookup and then it lists this. And common use case at the beginning is it has just a few rows in this visual table, but with extended usage over time, You could have hundreds or thousands of things in here and performance might degrade. There's any number of different problems that may happen eventually. And the answer is always going to be, can I get visibility? for the human to come in and see this part of the system. So if you can imagine any of these applications, we've defined the main pieces in that first document, we've defined them. flows of how things work now imagine that this was all made inside of a big factory our anatomy of the factory here is if it got created like, you know, a compact car, and you want to say, show me this component inside here, it's like, You know, you've got to pull out five different things just to go in and actually see where this pipe is going and how this thing connects to here. But this is a whole lot more than just like a compact car metaphor. This is almost like more like a set of conveyor belts inside of a factory. I don't want to just get into that compact car to see where the hose is going. I want to see the actual water flowing through it. I want to see the thing that I care about, which is, you know, users might be creating objects, or they might be giving input, or they might be storing settings. The thing that users are caring about is something that flows through the pipes, quote-unquote, pipes inside of the application. So the pipes of the application itself have objects in it. If it is a front-end user interface component that is listing all the widgets in a big list here, the objects are the actual objects. widgets that get read from whatever backend state full storage and then get put into memory and then get shown on the front end. If I'm having performance issues on speed because there's hundreds or thousands of entries in this thing now and it's downloading all the backend thing. By me being able to go and peek into that thing to see that pipe is coming through, I see, okay, I now have the payload that just came in here. Wow, that has 20,000 rows in it. And each one of those ones doesn't just have the data I need to visually show this. it's got the entire full depth of it because later on down the line, I see that there's a whole lot of, you know, things that were just pre caching some stuff in here, but I, I can't see what that problem. So this AI to human visibility layer, is something I want essentially to be, if this is a factory, I want there to be nice aisles in the factory that... The AI can easily give the human a tour to say, yeah, here's this thing that goes on here. You know about the front end component. Those are domain driven design ubiquitous language. You know what that is. The whole back end of this is working may not be obvious just from the description of the component on the front end. It could be like that I'm, you know, connecting through this thing and I have this connector to do an asynchronous call to this, which might persist somewhere else or something. That's a piping that needs to be explained. So on the AI tour of that one, it would be literally in this section of the thing. If this is getting a list of objects and loading it into memory and then putting it in here, it needs to actually be able to show, you know, when the robot on a factory floor idea, it needs to be able to say, hey, come over this corner. you can see the objects flying over from here and it has an asynchronous cue thing here to let the interface show a loading spinner until they're finished and this thing has a little indicator when it turns it on. And I'm not thinking that this is a, you know, a big visual factory that you're making inside of the thing. The next thing I want to try and describe is how this can actually be done. It's a set of strategies to try to create debuggable, um, attributes inside of the thing for visibility of an otherwise back-end working system. Firstly, we need to have a specific aspect of every individual part of the components. For every front-end UI component, for every back-end component, maybe in the environmental ones, maybe not, we need to have a... AID aspect that we can add to each one of those. And it's basically a framework that can be turned on and off. The main object of turning it on and off is that by doing observability, it can degrade performance. I don't want to have any degraded performance. If we can. So each one of these needs to have a flag where we turn on or off visibility things. But if I turn it on for a given backend thing, it needs to be able to start gathering metrics on it. So if we're debugging something that involves a front-end UI component on step two of a critical path, you can tell right from the critical path, these are the three things that need to be... examined. So each one of these different front-end components and back-end components needs to have a flag of is instrumentation, AI to human visibility mode turned on. As soon as that is turned on, it needs to then start logging information. So this needs to be part of, I think, any of these applications that are built with AI is to have an adequate logging mechanism that can be recording point-in-time things that happen during the flow of a process. So each one of these ones, if I want to debug a certain problem, it should be which of these levers do I want to throw to turn on which instrumentation things. I think it's okay for the general logging system to maybe be doing some nice low-level, not risking performance logging. And that's fine. But if I want to be able to turn on individual logging things, and I think this might eventually get to a point where... I believe one of the hallmarks of all these AI to human built things is going to be that you have a settings app somewhere in the application where you have a developer mode you can turn on. That developer mode you can turn on, what enable buttons and critical places did to let you debug and look into something. But even those visual differences should probably be gated behind these component things. So I think that turning on developer mode should turn that on, and then it should give you flags to turn on which different aspects of the system. So we're literally naming each one of those components, each one of those backend modules, and by turning them on there, that is the thing that flips the switch. So by flipping that switch, you can turn on the front end. The AI itself can turn those on if it knows we're debugging stuff and it can go enable that. That's the other aspect of AI and human programming is that there's a chance that we're going to have AI be able to run the thing, have this enabled, and have a way for it to get the logs out. That can generally take one of two forms. One, you actually make it so that the human can turn it on, and then they need to have some copy thing, which copies all the logs in a compact enough format. If you're, like, dumping giant objects, it can't have that full object in there. It's got to have some reduced amount, so I can copy and paste it without being a billion tokens. and the human can copy and paste it right into your AI conversation. The other side, though, we need to figure out security in between route which has not been figured out yet should be an ai letting it actually export its own stuff and be able to go just fetch that so theoretically a human can say uh yeah i can say go run the thing and then human can say all right i tried that the ai can then go check to see the session of whatever logging stuff it needs to to check and it itself can pull in the law raw logs there that eventually is going to be something we want to get set up and make it smooth because that allows Retrieval Augmented Generation. Where it can have all the more detailed logs. We don't have to worry about slimming it down as much. We can let the AI browse through the logs that are available. within that. So some automated log sharing ability because users will be using this application on who knows what client and having some integration where we can say, enable this integration, maybe it's something on the app, you actually enable something and give it a key and your app can share the key and it can go read it. But some kind of a log sharing telemetry communication system. But that's the secondary one. The primary that should always exist is the copy. You'll be able to copy detailed logs out to be able to paste it into your AI context. So, each module, front-end or back-end, should have enable AI to human visibility mode. That might degrade the front-end processes. I don't think I will advocate for these kind of debugging things actually showing or changing the visible behavior of the application. For example, so I've done debugging AI in the past, and as soon as I turned on developer mode, it started putting like... you know, status loading one, two, three, four, you know, as a visual div that it added on the page for the user to see. I don't advocate for that. I think all I want is that it's grabbing the data, it's logging the stuff. It's possible. That could be a case if the user specifically wants to see the detailed things here. Another thing that the debugging mode is often commonly to... The main objects that get handled by the system have some kind of a visibility thing. So if it's an object that's in the system and it's in the back end, this is probably a thing for all the back end modules. If they have persisted models. There needs to be a discipline of when the human wants to. So this is probably a whole aspect of the AI to human visibility thing is. Any important objects of the system that are defined in the domain-driven design need some way to browse those raw objects in the back end. And I think we could work out how to make it effective. The natural output is just to dump the JSON right to the screen. this takes a little bit of effort of how to show it effectively on the screen. Because without trying to create a giant list, and sometimes that's happening on an iPhone, and you just can't see almost anything. So it does need to eventually be able to give the full raw field nothing hidden. But maybe those are done with expandable things. Maybe it's view each entry with some kind of a visual image. So it does need to eventually be able to give the full raw field nothing hidden, but maybe those are done with expandable things. Maybe it's view each entry with some kind of a visual layout, and then you can click to view the raw JSON for each one of those ones. But the back-end individual pieces, I think should have a debugging mode. I think when you turn on debugging mode, there should be at least on the home index screen of the application, some kind of a bug icon, emoji or something. By clicking on that, it opens up a debugger panel. That debugger panel lets you browse these different module or the different objects within modules to list all of them, even the ones that are maybe in the database, but are status of deleted or something. But that's a specific section of this AI to human visibility document that needs to specifically be called out. What is the visibility strategy for showing persisted objects? What is that for each one of them? The most important things humans want to be able to see. What is the simplified view I can show to make it easy and quick to browse this? What general things would I show? What's some idea of layout for each one of those? But then what is the button I can click on to view the raw JSON dump for a given object. We can just be smarter about that. So the other half is the logging things where I can enable logging to see what's going on. And the third is logging reviewer. I think that for right now, logs get captured, just need to have a log viewer I can view the logs in the page. Logs do need to be persisted into some long-term logging session-based thing. So if I load an application, it should start logging for that session. Those logs should be available to be viewed in there. But if I reload the page to do it again, I need the past logs also stored in there so I can go to the previous log session, be able to copy those things out from that. But the logging views themselves, I need to be able to kind of walk through a tour as to what happened during the given session. And generally the things that end up showing the log should be controllable, the logging views themselves. I need to be able to kind of walk through a tour as to what happened during the given session. And generally the things that end up showing the log should be controllable by me controlling which components and modules that I have enabled logging on. If I've done some work on choosing to only debug those ones, theoretically by following through the logs, I might, you know, maybe there's eventually a debugging level if we decide that there's too much detail in here. I need to see it in a simpler model. But making a harness at least so that humans can view this, that that logging view. is effective. So in this AI to human visibility markdown file, each module should be annotated as to what the main... If it's a front-end thing, what are the main front-end steps and modes and things that could be logged? Generally, anything that's going to have an asynchronous delay on it, anything that has... data involved with it, you know, those are points to be logged, to be enabled for the debugging thing. Generally, starting events is to when something loads, and then ending events, once it's actually loaded or adequate. But if total elapsed is sometimes also valuable, you know, like the total amount of time from the first content, first paint or something on a list interface or something like that. That's sometimes useful to see as a metric on there. But each module on the front end described the different events that would need to be debugged. And then give an idea for each one of those ones about how many iterations in the average load, page load that are going to happen. But each module on the front end describe the different events that would need to be debugged. And then give an idea for each one of those ones about how many iterations in the average page load that are going to happen there. Because we need to call out in advance if there's 20,000 of these ones that happen on a given page load, or something that happens on D-Balance or something. that's going to be too noisy and we don't want to do those. So each of those telemetry points for each of the front end components needs to be called out for the back end. Any different methods that happen as part of a different flow there for the back end need to be called out as to what things are useful to show on each one of those events. because they don't need to show the full object, like if it's an API on the back end. It shouldn't be API foo was called, these were the arguments passed in, and then here was a response object. That is way too generic. Each one of these needs to be focused on the idea of what practically would be the best way for humans to be able to understand what is happening on the back end, what is happening on the front end. and where. And one final thing when you're viewing the logs, and we have turned on logging for different individual components, there should be a filter view so that I can hide and show things. If I decided to log for three components, and as I'm browsing it, I decided that, One of these is way noisy and I don't really want to see it. I should be able to turn those. I'll hide and show those to hide those in the interface. But this human AI to human visibility layer is essentially effectively the tour of the factory. I can go through and see what the logging telemetry is by looking at this document. By having this document defined, the AI should know exactly what it needs to build. And at the end, when the actual application is running, the factory floor is whirring and it's doing all of its steps. and some objects are flowing through conveyor belts and through pipes. Theoretically, a human could then just walk through and look at it. Anyway, this overall is a framework. that I think can be applied to any given application from the different user interfaces to communicate the value, break it down into pieces, but then also on the application that gets built, the human needs to be able to walk the floor. They need to be able to see the factory. They need to see the pipelines. They need to understand generally what is the different parts of the system here. Go and actually build these two documents. Don't actually change any code to start making it so. And this needs to be a document, essentially. For right now, just define for this given project that I am pasting this in for it. But just define what you think these documents are. and the third document at the end will just be a recommended refactors to be able to accomplish the goals in these two And the third document at the end will just be a recommended refactors to be able to accomplish. the goals in these two documents. Once again, the main document you're going to make is critical paths Let's see, we'll call it flows and parts maybe. It's got the critical flows and it's also got the parts, the front end components, the back end modules. So flows and parts markdown file and then an AI to human visibility markdown file. That are two things that would be in the documentation folder. in any of these applications that are going to be built on this one. And for right now, we're going to have you make those. After we've done some refining, then we'll have you look at several different projects that I've done this on, look at the outputs, and we'll try and standardize on what these things should look at more specifically. Can you go and set that up for this repository for me? Thank you.

<<<<<<< HEAD
=== Prompt 3 (reader stalls around Jacob 4/5; need richer load/scroll logs) ===

<user_query>
{
  "sessionId": "session-1775370434537",
  "visibleEntries": [
    {
      "time": "2026-04-05T06:33:56.561Z",
      "level": "debug",
      "module": "domain.readerEngine",
      "event": "reader_chapter_load",
      "summary": "Loading chapter into reader buffer",
      "refs": {
        "bookId": "jacob",
        "chapter": 5
      },
      "details": {
        "seq": 59,
        "mode": "append",
        "loadedCount": 2
      }
    }
    // ... many repeated reader_chapter_load entries for Jacob 5 omitted in this log transcript ...
  ],
  "filters": {
    "modules": [
      "domain.readerEngine"
    ],
    "levels": [],
    "search": ""
  }
}

=======

when I load the reader view, I am scrolling down to the end of Jacob and it stops on Jacob for and doesn’t seem  five at  and it should let me start scrolling right onto the next book as well. The logs for this didn’t seem detailed enough I was expecting to see an attempt to load the next thing and see that it failed maybe even  on where it thinks the current scroll place is so that you can see  I am describing to you in the logs.
</user_query>

=== Prompt 4 (add AI-Modularization-Standard doc as-is) ===

<user_query>
# AI Modulization Standard

This standard exists to keep AI-assisted software development grounded in architectural truth rather than momentum. It is designed for teams that move quickly, use AI heavily, and still want systems that remain understandable after many iterations. Its purpose is to make structure explicit, make debugging operational, and make refactoring accountable. The goal is not documentation aesthetics; the goal is durable clarity.

The central idea is that software quality should be observable in three ways at all times: by reading the flow narrative, by reading module contracts, and by reading runtime evidence. If any one of those three is weak, the system becomes guess-driven. When all three are strong, teams can evolve the product without constant fear that hidden couplings and invisible behavior will accumulate.

This document is intentionally portable. It should work across projects and stacks. It should not be coupled to where code currently lives. It should describe what must remain true even when implementations are reorganized. In practical use, this standard is read first, then used to produce and maintain three evergreen companion documents: Flows and Parts, AI-to-Human Visibility, and Recommended Refactors.

## Non-negotiable pillars

- Every major front-end part and back-end module must have explicit ownership, clear boundaries, and a stable contract that other parts are expected to respect.
- Application architecture must be explained in ubiquitous domain language first, then implementation details can be layered underneath where useful.
- One critical flow and at least one secondary flow must be clearly described as complete value journeys, not fragmented technical snippets.
- Visibility and debugging must be designed in from the start, not bolted on after failures appear in production.
- Instrumentation must be safe by default, with negligible overhead and no surprise behavior drift when debug mode is disabled.
- Refactoring priorities must be tied to adherence gaps against standards, not only intuition or convenience.

Those pillars are small enough to remember and broad enough to guide decisions. When a change weakens boundary clarity, flow clarity, or observability safety, it should be treated as architectural regression even if it appears to improve short-term delivery speed.

## Flow-first architecture as the anchor

Most software drift begins when systems are described as inventories rather than journeys. Users do not experience repositories; users experience outcomes over time. For that reason, this standard insists on flow-first architecture communication. A critical flow is the main path that justifies the product. A secondary flow is meaningful additional value that users rely on regularly. A tertiary flow often covers operations and diagnostics, which become central once the app is under load or in active maintenance.

Flow narratives should be concise, readable, and complete enough that an engineer can map them to behavior without reverse-engineering the entire codebase. They should identify the major front-end part involved, the major back-end module involved, and why that step matters to user value. They should avoid becoming file-path narration. If implementation is reorganized, the flow document should still describe the truth of the product.

## Module boundaries as a maintainability discipline

In AI-assisted development, boundary erosion happens quietly. A module starts with a narrow purpose, then absorbs orchestration logic, then acquires ad-hoc dependencies, and eventually becomes a “do everything” surface. This standard exists to stop that drift early. Module clarity is not merely a clean-code preference; it is a scaling requirement for human understanding and AI correctness.

Each major module should be able to answer four stable questions: what it owns, what it exposes, what it depends on, and what it does not own. Back-end modules should align with business or use-case domains, while shared infrastructure should remain infrastructure rather than becoming accidental domain owners. This prevents hidden coupling and keeps interfaces meaningful.

When boundaries are clear, diagnostics improves automatically. A failure in a flow step can be mapped quickly to a likely contract boundary. That shortens incident resolution loops and lowers the cognitive burden for both humans and AI assistants.

## Visibility philosophy: a guided factory tour

A reliable system should be explainable while it is running, not only after static code review. This standard treats observability as the ability to give a human a guided factory tour: where data entered, what transformed it, where state was persisted, what paused, what failed, and what eventually reached the user.

That is why every major part and module needs an explicit visibility posture. In developer mode, humans should be able to enable targeted instrumentation rather than flooding the entire app with logs. They should be able to inspect key persisted objects in both summary and raw forms. They should be able to filter event noise by module and concern. And they should be able to export focused diagnostics for AI collaboration.

This is not only about logs. It is about navigable evidence. A meaningful visibility layer tells a coherent story of behavior across time, and does so in a way that can be reused across debugging sessions rather than rebuilt from scratch for each incident.

## Performance-safe observability by default

Debug power that degrades normal user experience is not acceptable architecture. The default mode for observability must be low overhead, low intrusion, and functionally transparent. If instrumentation is disabled, user-facing behavior should remain stable and performance should remain effectively unaffected.

When instrumentation is enabled, some overhead is expected. However, that overhead should be intentional, bounded, and scoped to selected modules whenever possible. Debug mode should not silently mutate business behavior. Visual debug overlays may occasionally be useful, but they should be explicit tools, not unavoidable side effects.

Observability failures must also be isolated from core value delivery. If log persistence fails, the product should still perform its primary function. Instrumentation supports the system; it should not become a hidden dependency that can break core workflows.

## Required document lifecycle

- Read this standard first and confirm that product purpose has not shifted in a way that changes flow priorities.
- Produce or update Flows and Parts as the implementation-agnostic source of value journeys and module intent.
- Produce or update AI-to-Human Visibility as the source of instrumentation, object inspection, filtering, and export expectations.
- Produce or update Recommended Refactors as an adherence report that scores current reality against the first two documents.
- Implement code changes and then re-score adherence so architectural claims remain evidence-based.

The three companion documents have distinct jobs. Flows and Parts defines architectural intent in domain language. AI-to-Human Visibility defines what evidence exists and how humans can use it. Recommended Refactors defines the gap between intended architecture and current implementation. Keeping those roles distinct prevents confusion between strategy, telemetry design, and execution backlog.

## Adherence over wish-listing

A recurring failure pattern in technical planning is converting every observation into an unstructured backlog. This standard rejects that approach. The refactor document must be an adherence report. Each key criterion should be rated Yes, Partial, or No with clear evidence and a next action when not yet Yes. Broad grades such as A through F can be used for fast communication, but they should never replace criterion-level evidence.

The purpose of scoring is not bureaucracy. The purpose is to stop debates that have no shared frame. When two engineers disagree about readiness, the system should be able to point to flow coverage, boundary clarity, visibility completeness, and performance-safe instrumentation evidence. That turns architecture quality from opinion into testable posture.

Healthy systems eventually show long runs of Yes on core criteria, with temporary Partial scores only during active transitions. At that stage, the refactor document should explicitly acknowledge high adherence rather than pretending there is always major correction work left.

## High-value litmus signals

- New contributors can explain the critical flow and name the responsible parts/modules without reading a large volume of code first.
- Flow documents remain valid after implementation reorganization because they are written in domain language rather than file-location language.
- Developer mode exposes selective instrumentation controls for front-end parts and back-end modules instead of all-or-nothing logging.
- Key persisted objects are inspectable in both quick summary and raw detail formats during real debugging sessions.
- Log review supports filtering that isolates one noisy module without destroying overall sequence understanding.
- Diagnostics can be copied or exported in bounded form that AI tools can consume without token explosion.
- With debug mode off, observability overhead remains negligible and user-facing behavior remains stable.
- Critical and secondary flows are verifiable with automated or defined repeatable manual protocols.

## What this standard is designed to prevent

The failures this standard targets are familiar: architectures collapsing into single orchestration surfaces, domain terms being replaced by implementation trivia, logging becoming either uselessly noisy or too sparse to diagnose, and refactor conversations drifting into preference arguments. The standard prevents these outcomes by enforcing one loop: state the intended flows and module boundaries, state how behavior is observed, measure adherence, close gaps, and repeat.

In other words, it reduces the gap between “the app works right now” and “the app is understandable, diagnosable, and maintainable under continuous AI-assisted change.”

## Practical adoption posture

Projects do not need perfect architecture on day one to use this standard. Start with one clean critical flow, one meaningful secondary flow, and explicit module naming in ubiquitous language. Define the visibility layer at a practical level: what can be toggled, what can be inspected, what can be filtered, and how diagnostics can be shared. Then score adherence honestly and prioritize gap-closing actions that improve multiple criteria at once.

As maturity increases, tighten event schemas, strengthen module-level toggles, formalize object inspection UX, and improve flow verification coverage. Where full automation is not realistic, define stable manual protocols so quality claims remain falsifiable.

The point is steady convergence. Over time, the standards and the system should reinforce each other: clearer flows produce better instrumentation, better instrumentation reveals better refactor priorities, and better refactors preserve clearer flows.

## Definition of success

This standard is successful when architecture can be explained without hand-waving, when runtime behavior can be diagnosed without emergency instrumentation, and when refactor priorities are driven by adherence evidence instead of intuition alone. It is successful when module boundaries remain legible through change, when debugging capability does not tax normal users, and when teams can confidently evolve the product without losing structural integrity.

Most importantly, it is successful when the standard remains alive: continuously consulted, continuously reflected in the three companion documents, and continuously validated against real system behavior.


==========

Above is AI-Modularization-Standard.md, to add to the docs, as-is.
</user_query>

=== Prompt 5 (deeper narrative visibility + matching instrumentation) ===

<user_query>
All right, I think we're lacking some detail, some of the real intended meat of this standard. which is highlighted by the issue that I called out for you. So, the AI to human theory, document a high a human visibility document the goal was for it to have a way to explain well to the human what exactly is going on here. But it needs to not just be like saying simply it's a view that when you scroll up, it loads a previous and the next one. It needs to give you some thoughts as to why this is tricky. Explaining the marvel behind what it's doing here. Maybe not every section has this type of thing on it, but... To some degree, there's a story. And then, based off of that story... That determines what metrics debugging is on them. So, the fun complex piece of mechanism that is on That page, the reader review in our specific application here, is that it's an infinite scroller. So it has a complex set of events. As you're scrolling up and down, there's some thresholds to get crossed, and that threshold causes events to happen. So threshold like, you know, the loading of the next thing. And then talk about how You know, going from the end of one book to the next book, you know, needs to happen seamlessly too. And the only thing we don't do is the end of the work. We don't go all the way from the Book of Mormon to Doctrine and Covenants or something like that. But it should just keep on continuously scrolling there. So whatever the magic is of that system, that's the point. And I think right now we're doing a disservice of the three different apps that I've applied the standard to. I've gotten very randomized, different kind of... output. Sometimes it gives me a table, you know, talk about random stuff and visibility, blah, blah, blah here. And then it just has a raw table that it has like the module name on one column. And then another column has the individual events. And that's the only thing, the only thing in that document that talks about that specific module by module is this table. And that format is just simply never going to work to sell this type of thing to it. To say, let me explain how this, I'm basically giving you the sell that this needs to essentially do. And needs to do this on every single module. So we need to update the actual AI modularization standard as well to sell. somehow encapsulate this, maybe with examples, but I think we can have qualitative statements explaining why it's needed, what the contents are like. So in this one, I think I would say something like the reader view is a challenge because first off, we have the current scroll position on the current page. And we... know where things are in there, but it's, you know, based on the wrapping of the text, it's dynamic, so knowing where we are, there is some amount that is up off screen, and there's some amount that is below off screen. We want to make sure that the amount above and below off screen is a certain amount. Knowing how that works only works if we have some constant watching of a threshold. When the threshold gets to this point, our goal is that it will... keep, you know, three full screens of context loaded. That means that we have to load content to contain three full screens. That means we have to load those and then see what pixel dimensions those are. And if it's not high enough, we load more. And that loop has to be going in the background. If you're scrolling backwards, that threshold also moves. So we've got upwards and downwards scrolling to keep in mind. But the challenge is as well as we load and unload each different chunk, we need to do a measurement as to where our current scroll is if we are dropping out a... section from above and it's flowed from above by adding a new section that it's going to change the flow. It's going to be a different place down below in the scroller. So that infinite scroller has to somehow do that. So it has to keep track of the current position pre-loading the new chunk and then load the new chunk in and then see how big it is and then... append it or something like that. Whatever the actual process is, we need to walk through that whole thing and explain why it's doing it, how it's working. And then, after it's explained how it's working, to say, these are the things that you can look for in logs. You know, this, this, this, and this. This would show that. This would show that. To show this is a healthy process that's working just fine. So that's the idea of each one of these components. Every one of them needs to basically explain what's going on. Most of the time it's a lot simpler than explaining an infinite scroller. But still, explain what's going on and where these events fire. So reading through this visibility section on each visual front-end component as well as a back-end component. you know what you're going to be looking for. See if you can update this. See if you can figure out what is going on in this. And then go ahead and actually see if you can... after you've updated the documents for this reader front-end visibility thing, maybe even the back-end thing if that's needed to, add that level of detail to all the components and then go and actually update the code to instrument to those details that you have now made in the documentation.
</user_query>

=== Prompt 6 (convert visibility to folder + deep scenario docs) ===

<user_query>
All right, this needs some more rework. What I'm realizing is that the AI to human visibility layer... um yeah ai human visibility needs to be a subfolder not just a single file um because it really needs to tell a whole story on each individual module i think there needs to be a ai human visibility folder with a readme summarizing the basic stuff the goals and stuff you have at the top of the AI human visibility markdown file, but there needs to be a full AI human visibility story for each one of them. For example, this reader module, a front-end component, you know, it needs to be making it feel like I'm completely at home with understanding what's going on here. Specifically, it needs an in-depth scenario of like, Okay, imagine the following scenario. You have now loaded up 1 Nephi chapter 4, and you're in the middle of the chapter. This is the situation where it will have been preloaded. Walk me through that. Then, imagine first that I'm just manually scrolling down. There's a point which I should cross. and you know give it actual examples as to how many verses are in those chapters in that in that chapter and then maybe do first Nephi chapter five because chapter six is relatively short Maybe do 1st Nephi chapter 5 because chapter 6 is a relatively short chapter. Then you can have the example of I scroll down when I get to this certain point. Maybe give an example of the viewport height of an example iPhone and then how many total pixels high for an example that the current loaded content of chapter 5 is. Maybe chapter 6 was already within range, that it's already been preloaded, in which case maybe it's better to start with chapter four so we can show that when we get past a certain line, it's going to preload a next chapter to keep that certain amount. But then the sequence of events that happens to say, first, we're going to get this, we're going to load it here. I don't know the actual sequence, but I don't want to tell you the way to do it. I assume there's some industry standard way to do infinite scroller like this. However, this is how my brain works. I would load the main in view mode, in view with the content inside of it, and that I keep it a fixed position. I don't know, I might even do a scrolling thing inside of it. Maybe scroll top is the best way to do that, but keeping it in view to the correct level. The point when I have now loaded another section at the end is easy. You can just add more content down there and then see what the current rendered content height is after that. The trickier one is when you scroll backwards. If I scroll backwards and I get past to a certain point that I'm in the whatever threshold, your story just needs to give me those exact numbers as the threshold. You know, I've currently started the length of this. I only need to scroll up about 200 pixels or something or a thousand pixels or whatever until until I hit that threshold. Scrolling up is the challenge because I need to preload content from before, or a thousand pixels or whatever, until I hit that threshold. Scrolling up is the challenge because I need to preload content from before, where if I just insert that content into the current view, it will bump everything down. So what I think I need to actually do is get it so that I preload the content in a separate div, a separate floating thing that's not floating and pushing it down, let it render, get the actual height of it, and then either append it or just keep it separate individual panels that are all sliding individually. And then if there's a point when I need to, I can't go to a negative scroll top or something like that, then I need to at some point do a redo where I say, all right, I'm about to remove this thing at the top here where I'm unloading a chunk. That's the other thing. If I'm loading scrolling down then I remove a chunk from the top. I need to do that as a conscious point and do it, maybe to wait until the scrolling has stopped or something. So mid scroll, I don't end up shutting something out down like that. And then look at the height of the thing that I'm going to subtract from the thing and then rapidly delete that content and then set the new scroll top to the correct height. Maybe there's a way I can set the positioning whatever fixed during that point so it doesn't have temporary glitches it read does it stuff anyway those are those are the strategies that if I were designing this thing and I've done one infinite scroller in the past that I've made it all up myself I don't want you to follow my design if there's a more but if there's a better methodology out there for doing infinite scrollers so please research online to know that but then inside of the this special breakdown there you need to be walking through the whole scenario so know that but then inside of the this special breakdown there you need to be walking through the whole scenario so I can see actual example scenarios and what pieces are in place as you're walking through that you can call out the individual events that should fire to say when you are on the page you have 400 pixels up if you scroll before you hit that threshold well great give us that threshold out of the debug then as soon as they cross threshold this event fire saying load the next thing. That would have events saying chunk loaded, new height is such and such. And then appending this to there and then doing a swap on the other one or whatever the steps are, they should annotate what I should see in the debugging logs so I know what when I go view the thing, what actions I can perform. In other words, I can scroll within about 400 pixels. And then secondly, what events I'm going to see when I get to certain things. I should be able essentially see that. That's the equivalent of this factory floor thing where I can see the conveyor belt moving along. I can see what's going. I can see the object. That's the level of detail every one of these components needs to be set to so that we know what events happen, how they interact. Telling me the whole story is a complete story. That's why this needs to be expanded to a full document for each one of them. And things that are technically complicated like this reader thing need to be expansive and large to, you know, basically set ability for a layman to read and understand well and be at home seeing the actual document that the debug values that are coming out.
</user_query>

=== Prompt 7 (clarify UI vs backend modules; deepen all module stories) ===

<user_query>
I don't know that this is what I was asking for. At least in the flow and parts markdown file it should, I think, you may clarify it in there a little bit if you want, but it should be really clearly identified. Like if it's hand wavy go fix it there. But there There should be a very razor sharp definition as to what the individual places, pieces that we have broken the app into. There are two types. There are backend and there are UI ones. So UI chunks or modules or components or whatever, front end systems or, it looks pretty close to what you've got in here, but the backend ones, I'm not sure what this domain reader engine, maybe that's supposed to be the backend thing, but the actual UI reader view should be. I mean, maybe it's a mix between those two. Is that what the domain reader engine is about? I mean, the backend thing, if it really is the domain reader engine thing, right? The domain reader engine is the place where you were documenting all this. This is where you put all this expansive detail. Domain is just wrong. Domain-driven design. Domain is the higher level of the different thing, but these are components of that. I don't know. Maybe we need to be more clear. If you call it domain-driven design, then you call this domain. That's confusing. I'm thinking module, back-end module, front-end module. you Main-driven design, then you call this domain. That's confusing. I'm thinking module, back-end module, front-end module, UI module. We need to be more clear as to exactly what we're calling which thing and make sure we update the standards to be more clear about that. But the parts and flow, flows and parts, should be really clear as to what the individual concrete things are. If there are 10 pieces, then that's great. every single AI human visibility should have a separate piece but each one of the AI human visibility things especially like the UI and the front end stuff like I don't understand how it's possible that the back end thing, I thought that the reader I don't know, maybe he can explain what the reader engine is doing. Maybe that is the division of the two. But it certainly seems like it's a UI thing where it's literally updating the visual things on the front end if there is a scroll of a certain height or something. That certainly seems like a visual front end thing. The big division is front end versus back end in an app where the back end is still running inside of the view here. The main difference is that the back end should be pretty agnostic. If you wanna somehow abstract where things are, I figured the back end was keeping track of current loaded stuff maybe. I don't know how you're gonna divide this to front end to back end. Maybe this is something that you're trying to, again, the flows and parts should be how this thing should be designed, not necessarily how it is coded today. Maybe that's what you need to fix. But the flows and parts should be explaining really well be designed, not necessarily how it is coded today. Maybe that's what you need to fix. But the flows and parts should be explaining really well what the heck the difference is between a front-end UI reader view and the domain reader engine, or is really a back-end piece. Just to understand that the abstraction is the key here. Theoretically, anything in the back-end won't have pixels mentioned in it. It won't have scroll top mentioned in it. It won't have things like that are essentially UI-centric things inside of it. They should be theoretically unit testable without a browser because none of that stuff should really matter. Now, if there's a reason, maybe you could be communicating the heights and what things are loaded is a backend domain of what's loaded in the different current domain here. That could be the current state where you're managing it or something in there, but like the front end, You need to define that more clearly. So please revisit flows and parts if you need to. Identify if there's a backend need, a frontend need. I could totally see that the reader engine is keeping track of what things are loaded, what things are in view, what thing is the current hovered over highlighted thing. And then maybe you're updating that and telling the backend engine, like current highlighted over thing is this. and then back end things could be, you know, keep you track of it. But like the front end, the actual scroller thing in the front end, those controls, all that's got to be on the front end. But the back front end, telling the back end what to load, it needs to be the front end that's doing this sensitive thing of saying, keep track of what the threshold is. But pixels can't be mentioned in the back end thing. It doesn't make sense. That is a front end concept that can't be tested in the back end in isolation. They need to be a separate backend needs to be a separate isolation. Make sure that these are called out end concept that can't be tested in the back end in isolation. They need to be a separate back end, needs to be a separate isolation. Make sure that these are called out much more clearly in the AI standards thing, because these are often for front end applications where you have a division of these two things. Modules need to be completely agnostic of visual things that could be theoretically used in two different front end things that this module could be done that allows it to be testable from the back end with pseudo visual simulators or something like that. So I think maybe there's some rehashing that needs to be done there. But once you have that in place, it makes sense to have the kind of deep dive walkthrough thing that we're talking about here. But each one of these modules, back end or front end, should have the same deep dive walkthrough. A lot of these other ones you're giving me are not deep. I mean, when you're making like the UI books view and you're giving me the three names of the signals to watch, that is not telling the story. That is not explaining the entire exhaustive flow as to what things happen. This UI books view is used in this thing. It's loaded in this case. It's pulling this thing. This is the thing it's doing. Mainly the job is to keep track of this state. It needs to be telling the stuff and then the signals are mentioned during each one of those stories. So you're not expanding each one of those ones. They should all be specifically expanded. And this needs to be one of the standards in the AI standards thing. If you had that as an AI modularization standard, what these AI human visibility sub documents are for, and if you looked at that standard and you compared it for each one of these individual ones, that should have failed really badly on these ones because they are not at all what is being described here. Make sure that standard is clear. Make sure that it is asked to go re-evaluate each one of these files to see if each one of them matches the standard.I don't know that this is what I was asking for. At least in the flow and parts markdown file it should, I think, you may clarify it in there a little bit if you want, but it should be really clearly identified. Like if it's hand wavy go fix it there. But there There should be a very razor sharp definition as to what the individual places, pieces that we have broken the app into. There are two types. There are backend and there are UI ones. So UI chunks or modules or components or whatever, front end systems or, it looks pretty close to what you've got in here, but the backend ones, I'm not sure what this domain reader engine, maybe that's supposed to be the backend thing, but the actual UI reader view should be. I mean, maybe it's a mix between those two. Is that what the domain reader engine is about? I mean, the backend thing, if it really is the domain reader engine thing, right? The domain reader engine is the place where you were documenting all this. This is where you put all this expansive detail. Domain is just wrong. Domain-driven design. Domain is the higher level of the different thing, but these are components of that. I don't know. Maybe we need to be more clear. If you call it domain-driven design, then you call this domain. That's confusing. I'm thinking module, back-end module, front-end module. you Main-driven design, then you call this domain. That's confusing. I'm thinking module, back-end module, front-end module, UI module. We need to be more clear as to exactly what we're calling which thing and make sure we update the standards to be more clear about that. But the parts and flow, flows and parts, should be really clear as to what the individual concrete things are. If there are 10 pieces, then that's great. every single AI human visibility should have a separate piece but each one of the AI human visibility things especially like the UI and the front end stuff like I don't understand how it's possible that the back end thing, I thought that the reader I don't know, maybe he can explain what the reader engine is doing. Maybe that is the division of the two. But it certainly seems like it's a UI thing where it's literally updating the visual things on the front end if there is a scroll of a certain height or something. That certainly seems like a visual front end thing. The big division is front end versus back end in an app where the back end is still running inside of the view here. The main difference is that the back end should be pretty agnostic. If you wanna somehow abstract where things are, I figured the back end was keeping track of current loaded stuff maybe. I don't know how you're gonna divide this to front end to back end. Maybe this is something that you're trying to, again, the flows and parts should be how this thing should be designed, not necessarily how it is coded today. Maybe that's what you need to fix. But the flows and parts should be explaining really well be designed, not necessarily how it is coded today. Maybe that's what you need to fix. But the flows and parts should be explaining really well what the heck the difference is between a front-end UI reader view and the domain reader engine, or is really a back-end piece. Just to understand that the abstraction is the key here. Theoretically, anything in the back-end won't have pixels mentioned in it. It won't have scroll top mentioned in it. It won't have things like that are essentially UI-centric things inside of it. They should be theoretically unit testable without a browser because none of that stuff should really matter. Now, if there's a reason, maybe you could be communicating the heights and what things are loaded is a backend domain of what's loaded in the different current domain here. That could be the current state where you're managing it or something in there, but like the front end, You need to define that more clearly. So please revisit flows and parts if you need to. Identify if there's a backend need, a frontend need. I could totally see that the reader engine is keeping track of what things are loaded, what things are in view, what thing is the current hovered over highlighted thing. And then maybe you're updating that and telling the backend engine, like current highlighted over thing is this. and then back end things could be, you know, keep you track of it. But like the front end, the actual scroller thing in the front end, those controls, all that's got to be on the front end. But the back front end, telling the back end what to load, it needs to be the front end that's doing this sensitive thing of saying, keep track of what the threshold is. But pixels can't be mentioned in the back end thing. It doesn't make sense. That is a front end concept that can't be tested in the back end in isolation. They need to be a separate backend needs to be a separate isolation. Make sure that these are called out end concept that can't be tested in the back end in isolation. They need to be a separate back end, needs to be a separate isolation. Make sure that these are called out much more clearly in the AI standards thing, because these are often for front end applications where you have a division of these two things. Modules need to be completely agnostic of visual things that could be theoretically used in two different front end things that this module could be done that allows it to be testable from the back end with pseudo visual simulators or something like that. So I think maybe there's some rehashing that needs to be done there. But once you have that in place, it makes sense to have the kind of deep dive walkthrough thing that we're talking about here. But each one of these modules, back end or front end, should have the same deep dive walkthrough. A lot of these other ones you're giving me are not deep. I mean, when you're making like the UI books view and you're giving me the three names of the signals to watch, that is not telling the story. That is not explaining the entire exhaustive flow as to what things happen. This UI books view is used in this thing. It's loaded in this case. It's pulling this thing. This is the thing it's doing. Mainly the job is to keep track of this state. It needs to be telling the stuff and then the signals are mentioned during each one of those stories. So you're not expanding each one of those ones. They should all be specifically expanded. And this needs to be one of the standards in the AI standards thing. If you had that as an AI modularization standard, what these AI human visibility sub documents are for, and if you looked at that standard and you compared it for each one of these individual ones, that should have failed really badly on these ones because they are not at all what is being described here. Make sure that standard is clear. Make sure that it is asked to go re-evaluate each one of these files to see if each one of them matches the standard.
</user_query>

=== Prompt 8 (status check) ===

<user_query>
Did you finish? I don’t see any real changes that you made since my last comment to you.
</user_query>

=== Prompt 9 (status check repeat) ===

<user_query>
Did you finish?
</user_query>
=======
=== Prompt 3 (recommended refactors full implementation) ===

documentation/recommended-refactors.md

please review this file in the repository and the related documents. I want you to break it down into pieces make a plan and then systematically complete everything described in this recommended refractors document every single thing in that file get it all done.
>>>>>>> origin/main

=== Prompt 11 (prioritize docs merge; archive conflict snapshots) ===

<user_query>
I don’t know what happened on    I think I am OK with you looking at the changes and just picking whatever you want on the actual code change the main thing I want to get committed are the documentation files themselves so I would say save the merge conflict  in a directory that we commit for later  if we want to, but I just want you to get it ready to commit so that I can merge our documentation  in and then I will begin a complete pass on a full rewrite of all of the code
</user_query>
